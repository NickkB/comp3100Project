
public class App {

    

    public static void main(String[] args) throws Exception {
        try{

            // Utilities utilities = new Utilities();
            // ClientAction clientAction = new ClientAction();
            // ServerInfo servers = new ServerInfo();

            // utilities.initConnection();
            // JOBNEvent job = (JOBNEvent) utilities.readServerOutput();
            // clientAction.sendGETSAll(utilities.getOutputStream());
            // DATAEvent till = (DATAEvent) utilities.readServerOutput();
            // clientAction.sendOK(utilities.getOutputStream());

            
            // for(int i = 0; i < till.getCount(); i++){
            //     String temp = (String)utilities.getInputReader().readLine();
            //     String[] temps = temp.split("\\s+");
            //     servers.addServer(temps[0], temps[1], temps[2], temps[3], temps[4], temps[5], temps[6], temps[7], temps[8]);
            //     // utilities.readServerOutput();
            // }
            // clientAction.sendOK(utilities.getOutputStream());
            // servers.sortListByCoreCount();
            // servers.printArray();



        } catch(Exception e){System.out.println(e);}
    }
}



